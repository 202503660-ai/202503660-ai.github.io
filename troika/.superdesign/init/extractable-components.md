# Extractable components
No separate reusable layout components: single static document, no shared multipage shell. Keep inline for this single-page draft. Basic primitives: buttons (active), pills (selected), status badges (label), modal (open), cards (place).
